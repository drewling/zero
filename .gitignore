# Secrets & tokens — never commit
slack_app/config.env

# Python virtual environment
slack_app/venv/

# Runtime state (generated files)
drafts/queue.json
drafts/briefing.json
drafts/missed_today.json
slack_app/snoozes.json

# Logs
logs/

# Python cache
__pycache__/
*.pyc
*.pyo

# OS / editor
.DS_Store
*.bak
*.bak.*

# Sensitive credential backups + any OAuth client (the bundled one is injected at
# build time from outside the repo — see macapp/build.sh ZERO_CLIENT_SECRET).
client_secret.json
client_secret.json.bak

# Your real accounts (use accounts.json.example as a template)
accounts.json

# Your personal sender profile(s) that ground draft voice — never publish these.
# Only the *.example.md template is tracked.
knowledge/*.md
!knowledge/*.example.md

# Lock files
*.lock
drafts/*.lock

# Build output
macapp/build/

# Dashboard state (generated at runtime)
app/state.json
app/category_label_history.json
app/settings.json

# Editor / tooling dirs
.claude/

# Learning store (user-specific signals + rollup, generated at runtime)
learning/

# Session scratch (landing drafts, screenshots)
scratchpad/

# Local secrets (JEV / TypeSafe API key)
.env
# The Jev API key as saved by the app (Settings / onboarding). Same secret as the
# JEV entry in .env, just the path the packaged app can actually write to.
app/jev_key

# Isolated Claude Code config created at runtime by lib/llm.py (_claude_home).
# It holds .credentials.json — real OAuth tokens seeded from the login Keychain —
# so it must never be committed. Created automatically on first use; safe to delete.
claude-home/

# Generated by landing/build.sh: a staged copy of macapp/install-zero.sh so Docker
# can COPY it into the landing image. The canonical script is macapp/install-zero.sh.
landing/install
